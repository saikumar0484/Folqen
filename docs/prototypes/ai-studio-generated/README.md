# AI Studio Generated Prototype

This folder preserves generated prototype files that appeared in the workspace while Folqen was already using the verified `src/app` Next.js structure.

Important:

- These files are not active production code.
- `docs/prototypes/**` is excluded from TypeScript compilation.
- Do not move this code into the app without reviewing imports, dependencies, Prisma version assumptions, Tailwind version assumptions, and Folqen safety rules.
- The active Folqen app remains under `src/`.
