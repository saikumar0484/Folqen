# Review-Required Archive

This folder holds files that were not active imports at the time of the May 13, 2026 repository audit but may still have design/reference value.

Archived files are excluded from TypeScript and ESLint so they do not affect production builds. Restore a file into `src/` only after checking current imports, routes, tests, and UI direction.
